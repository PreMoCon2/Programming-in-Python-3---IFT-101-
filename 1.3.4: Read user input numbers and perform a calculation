# Read in a number from input and convert it to an integer, then assign it to the variable num1
num1 = int(input())

# Read in another number from input and convert it to an integer, then assign it to the variable num2
num2 = int(input())

# Add num1 and num2 together and output the result
print(num1 + num2)
